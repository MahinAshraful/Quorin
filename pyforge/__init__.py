"""Pyforge — low-latency ML feature serving via shared memory.

Public API surface is intentionally empty until Step 12. Import from specific
modules (e.g. ``from pyforge.schema import FeatureSchema``).
"""

__version__ = "0.1.0"
